### 项目需求

* mqtt client测试程序，连接在外网的mqtt服务器
* 每5秒钟向主题/test\_topic/upload\_from\_dtu发布消息"test info"
* 订阅主题/test\_topic/download\_to\_dtu，并且将收到的消息输出到终端
* 项目需要拷贝到有交叉编译环境的机器上编译
* 生成的程序需要从编译环境的机器上下载到目标板运行
* 用makefile编译项目
* 纯C语言
* 生成的可执行文件命名为mqtt\_client\_ai\_test



### 已经交叉编译好的mqtt client的库

* libmosquitto.a
* 头文件：mosquitto.h



### mqtt服务器

* mqtt.comway.com.cn:2883
* 用户名：user3
* 密码：pass3



### 本机环境

* mqtt发布测试程序：C:\\tools\\mosquitto-2.0.9-install-windows-x86\\mosquitto\_pub.exe
* mqtt订阅测试程序：C:\\tools\\mosquitto-2.0.9-install-windows-x86\\mosquitto\_sub.exe
* 本机已安装python，可以用python运行测试脚本



### 有交叉编译环境的机器

* ip：192.168.0.250
* ssh登录方式：ssh -i C:\\zcc\\zcc\_private\_keys\\zcc\_id\_rsa ubuntu@192.168.0.250
* 交叉编译：mipsel-openwrt-linux-gcc，mipsel-openwrt-linux-ld
* 项目目录：/home/ubuntu/mqtt\_client\_ai\_test/，如果没有这个目录就创建一个
* 生成的程序需要拷贝到/var/www/html/目录下，需要sudo cp
* 该机器提供http服务，目标板可以wget http://192.168.0.250方式下载生成的程序



### 目标板

* ip：192.168.0.251
* ssh登录方式：ssh -i C:\\zcc\\zcc\_private\_keys\\zcc\_id\_rsa -o KexAlgorithms=+diffie-hellman-group1-sha1 -o HostKeyAlgorithms=+ssh-rsa -o MACs=+hmac-sha1 -o PubkeyAcceptedKeyTypes=+ssh-rsa root@192.168.0.251
* 不能用scp拷贝文件到目标板
* 可以用wget方式从编译环境的机器上下载可执行程序

